# Dance Coach Platform (Эксперимент 1)

Полнофункциональная платформа для танцевального тренера, включающая бэкенд на FastAPI и фронтенд на Next.js. Поддерживает управление расписанием, запись на занятия, блог и аналитику.

## Структура проекта

Проект состоит из трех основных компонентов:
- **Backend**: FastAPI приложение с базой данных PostgreSQL и очередью задач Redis/Celery.
- **Frontend**: Next.js приложение с поддержкой SSR (Server Side Rendering).
- **Docker**: Конфигурация для запуска всего стека через Docker Compose.

---

## 🚀 Быстрый запуск с Docker (Рекомендуется)

Самый простой способ запустить весь проект сразу.

### Предварительные требования
- Docker и Docker Compose установлены.

### Инструкция по запуску

1. **Создайте файлы окружения:**
   
   Для бэкенда:
   ```bash
   cp experiment/backend/.env.example experiment/backend/.env
   ```
   *Отредактируйте `.env` при необходимости, указав настройки БД и секретные ключи.*

   Для фронтенда:
   ```bash
   cp experiment/frontend/.env.local.example experiment/frontend/.env.local
   ```

2. **Запустите проект:**
   
   Находясь в папке `experiment`:
   ```bash
   docker-compose up --build
   ```

3. **Доступ к приложению:**
   - Фронтенд: [http://localhost:3000](http://localhost:3000)
   - Бэкенд API: [http://localhost:8000](http://localhost:8000)
   - API Документация: [http://localhost:8000/docs](http://localhost:8000/docs)

---

## 🛠 Ручной запуск (для разработки)

Если вы хотите запустить компоненты по отдельности без Docker.

### 1. Запуск Backend (FastAPI)

**Требования:** Python 3.9+, PostgreSQL, Redis.

1. **Перейдите в папку бэкенда:**
   ```bash
   cd experiment/backend
   ```

2. **Создайте и активируйте виртуальное окружение:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Для Linux/macOS
   # venv\Scripts\activate  # Для Windows
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения:**
   Скопируйте пример конфига и укажите параметры подключения к вашей локальной БД PostgreSQL и Redis.
   ```bash
   cp .env.example .env
   ```

5. **Примените миграции базы данных:**
   ```bash
   alembic upgrade head
   ```

6. **Запустите сервер:**
   ```bash
   uvicorn app.main:app --reload
   ```

7. **Запуск Celery (для фоновых задач):**
   В новом терминале (с активированным venv):
   ```bash
   celery -A app.celery_app worker --loglevel=info
   ```
   Для периодических задач (Beat):
   ```bash
   celery -A app.celery_app beat --loglevel=info
   ```

### 2. Запуск Frontend (Next.js)

**Требования:** Node.js 18+.

1. **Перейдите в папку фронтенда:**
   ```bash
   cd experiment/frontend
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения:**
   ```bash
   cp .env.local.example .env.local
   ```
   Убедитесь, что `NEXT_PUBLIC_API_URL` указывает на ваш запущенный бэкенд (обычно `http://localhost:8000/api/v1`).

4. **Запустите сервер разработки:**
   ```bash
   npm run dev
   ```
   Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000).

---

## 📚 Основной функционал

- **Расписание и Слоты:** Интеграция с Google Calendar (опционально) и локальной базой для управления свободным временем.
- **Блог:** Полноценная CMS для публикации статей с поддержкой SEO (мета-теги, SSR).
- **Личный кабинет:** Регистрация, авторизация (JWT), просмотр истории занятий и абонементов.
- **CRM функции:** Сбор лидов с форм на сайте.

## ⚠️ Важные замечания

- В папке `experiment/docker` находится конфигурация Nginx, используемая при запуске через Docker Compose.
- Для работы интеграции с Google Calendar требуется настроить Service Account и положить JSON-файл ключа в папку бэкенда, указав путь к нему в `.env`.
